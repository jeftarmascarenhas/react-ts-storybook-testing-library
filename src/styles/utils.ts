import theme from "./theme";
export const colorSelect: any = {
  borderNormal: {
    primary: `1px solid ${theme.colors.primary}`,
    secondary: `1px solid ${theme.colors.secondary}`
  },
  borderHover: {
    primary: `1px solid ${theme.colors.primary}`,
    secondary: `1px solid ${theme.colors.secondary}`
  },
  borderDisabled: {
    primary: `1px solid ${theme.colors.grey}`,
    secondary: `1px solid ${theme.colors.black}`
  },
  colorNormal: {
    primary: `1px solid ${theme.colors.black}`,
    secondary: `1px solid ${theme.colors.white}`
  }
};
